# Installation

With NPM:

```bash
npm i @marceloclp/react-aware
```

Or with yarn:

```bash
yarn add @marceloclp/react-aware
```

ReactAware also depends on React 16+, so make sure you also have a compatible
version of React installed!
